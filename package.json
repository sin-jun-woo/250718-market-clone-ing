{
  "name": "2025-carrot-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.35.5",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "svelte-spa-router": "^4.0.1"
  }
}
